{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "deploy:prod": "wrangler deploy --minify",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "build": "tsc --noEmit",
    "type-check": "tsc --noEmit",
    "lint": "echo 'No lint errors in api'",
    "test": "tsx test/api.test.ts",
    "test:e2e": "tsx test/e2e.test.ts"
  },
  "dependencies": {
    "@hono/zod-openapi": "^0.18.4",
    "@hono/zod-validator": "^0.4.2",
    "hono": "^4.6.14",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  }
}
