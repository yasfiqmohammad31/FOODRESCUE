{
  "name": "foodrescue",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "test:e2e": "pnpm --filter api test:e2e",
    "deploy:api": "pnpm --filter api deploy:prod",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "turbo": "^2.10.12"
  }
}
